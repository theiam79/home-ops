---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: valheim
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secretsmanager
  target:
    name: valheim-secret
    template:
      data:
        PASSWORD: "{{ .PASSWORD }}"
        WEBHOOK_URL: "{{ .WEBHOOK_URL }}"
  data:
    - secretKey: PASSWORD
      remoteRef:
        key: "00000000-0000-0000-0000-000000000000" # TODO: bws id of VALHEIM_PASSWORD (README)
    - secretKey: WEBHOOK_URL
      remoteRef:
        key: "00000000-0000-0000-0000-000000000000" # TODO: bws id of VALHEIM_DISCORD_WEBHOOK (README)
