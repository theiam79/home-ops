---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valheim-server-files
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
