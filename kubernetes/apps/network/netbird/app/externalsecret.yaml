---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: netbird
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-secretsmanager
  target:
    name: netbird-secret
    template:
      data:
        # Whole server config lives here: see README ("Why the config is a secret")
        config.yaml: |
          server:
            listenAddress: ":8080"
            exposedAddress: "https://netbird.${SECRET_DOMAIN}:443"
            stunPorts:
              - 3478
            metricsPort: 9090
            healthcheckAddress: ":9000"
            logLevel: "info"
            logFile: "console"
            authSecret: "{{ .RELAY_SECRET }}"
            dataDir: "/var/lib/netbird/"
            disableAnonymousMetrics: true
            disableGeoliteUpdate: false
            auth:
              issuer: "https://netbird.${SECRET_DOMAIN}/oauth2"
              localAuthDisabled: false
              signKeyRefreshEnabled: true
              sessionCookieEncryptionKey: "{{ .SESSION_COOKIE_KEY }}"
              dashboardRedirectURIs:
                - "https://netbird.${SECRET_DOMAIN}/nb-auth"
                - "https://netbird.${SECRET_DOMAIN}/nb-silent-auth"
              cliRedirectURIs:
                - "http://localhost:53000/"
              dashboardPostLogoutRedirectURIs:
                - "https://netbird.${SECRET_DOMAIN}/"
            store:
              engine: "sqlite"
              encryptionKey: "{{ .DATASTORE_KEY }}"
            reverseProxy:
              trustedHTTPProxies:
                - "127.0.0.1/32"
  data:
    - secretKey: RELAY_SECRET
      remoteRef:
        key: "0420f2e9-3aa3-4b86-a567-b4ba00455b86"
    - secretKey: DATASTORE_KEY
      remoteRef:
        key: "95954252-87e4-4c2a-bddb-b4ba00456e34"
    - secretKey: SESSION_COOKIE_KEY
      remoteRef:
        key: "c0df6fb7-eef1-4b4d-a5e4-b4ba004f8032"
